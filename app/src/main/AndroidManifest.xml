<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.unab.toloza.cesar.tiendapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.EditarActivity"></activity>
        <activity android:name=".view.activity.AgregarActivity" />
        <activity android:name=".view.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.CoordinatorLayout" />
        <activity android:name=".view.activity.ListadoActivity" />
        <activity android:name=".view.activity.DetalleActivity" />
        <activity android:name=".view.activity.MainActivity" />
    </application>

</manifest>